# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`croogmandoo/BudgetApp`) is currently **empty** — no source files, no commits on `main`, and no build configuration exist yet. The name suggests a personal-finance / budgeting application, but no technology choices (language, framework, package manager, database) have been made.

When the first real code lands, update this file with:
- Build / lint / test / run commands (including how to run a single test)
- High-level architecture that spans multiple files
- Project-specific conventions that are not obvious from reading the code

Until then, there are no project-specific commands or architecture to document.

## Bootstrapping Guidance

If asked to scaffold the initial project:
- Confirm the intended stack with the user before generating files (frontend framework, backend language, persistence layer, deployment target) rather than guessing.
- Add the chosen package manager's lockfile on the first commit that introduces dependencies.
- Once a build toolchain exists, replace this "Repository Status" section with the real commands and an architecture overview.
