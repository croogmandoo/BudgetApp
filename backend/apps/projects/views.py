"""HTTP views for ``apps.projects``.

Scope: project / task CRUD, transaction linking, cost-rollup endpoints,
attachment upload delegation to ``apps.core``. No handlers yet.
"""

from __future__ import annotations
