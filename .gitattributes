# Normalize line endings to LF in the repo; CI runs on Linux
* text=auto eol=lf

# Force binary for files that should never be normalized
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.woff binary
*.ttf binary
*.eot binary
*.svg binary
